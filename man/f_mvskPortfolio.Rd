% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvskPortfolios.R
\name{f_mvskPortfolio}
\alias{f_mvskPortfolio}
\title{MVSK Portfolio Front-end (Mean–Variance–Skewness–Kurtosis)}
\usage{
f_mvskPortfolio(
  m1 = NULL,
  M2 = NULL,
  M3 = NULL,
  M4 = NULL,
  w0 = NULL,
  g = NULL,
  lb = NULL,
  ub = NULL,
  lin_eq = NULL,
  lin_eqC = NULL,
  nlin_eq = NULL,
  lin_ieq = NULL,
  lin_ieqC = NULL,
  nlin_ieq = NULL,
  href = NULL,
  kappa = NULL,
  relative = FALSE,
  param = NULL,
  options = list(),
  mompref = NULL
)
}
\arguments{
\item{m1, M2, M3, M4}{Moment inputs (see \code{\link{f_getmom}}). At least one
must be non-\code{NULL}.}

\item{w0}{Baseline weights. If \emph{numeric}, used directly; if not numeric
(e.g., a string spec), a preliminary solve via \code{\link{f_solvePortfolio}}
is performed to obtain \code{w0}.}

\item{g}{Moment target vector or function (see \code{\link{f_solveMVSKPortfolio}}).}

\item{lb, ub}{Bounds on weights (default: long-only \eqn{[0,1]}).}

\item{lin_eq, lin_eqC}{Linear equality constraint matrix and RHS. If omitted,
the fully-invested constraint \eqn{\mathbf{1}^\top w = 1} is enforced.}

\item{nlin_eq}{Optional nonlinear equality constraints (function).}

\item{lin_ieq, lin_ieqC}{Linear inequality constraint matrix and RHS (left
minus right \eqn{\le 0}).}

\item{nlin_ieq}{Optional nonlinear inequality constraints (function).}

\item{href}{Optional objective (function or string for
\code{mvskPortfolios:::get_href()}) used to define a relative constraint.}

\item{kappa}{Optional numeric vector; if provided with \code{href}, a solve
is run for each \code{kappa}, producing a path of portfolios.}

\item{relative}{Logical flag for relative vs absolute objective thresholding.}

\item{param}{List of extra arguments for the objective factory.}

\item{options}{\pkg{nloptr} options.}

\item{mompref}{Optional preference signs for moment directions.}
}
\value{
If \code{href} is \code{NULL}, a single list as returned by
  \code{\link{f_solveMVSKPortfolio}}. Otherwise, a list with components:
  \code{w} (matrix of weights over \code{kappa}), \code{delta} (vector),
  \code{moms} (matrix), and \code{constr} (constraint values).
}
\description{
User-facing convenience function that sets defaults (bounds, full-investment
constraint) and calls the appropriate solver to obtain a single MVSK
portfolio or a path over a grid of \code{kappa} values when an additional
reference objective \code{href} is supplied.
}
\examples{
\dontrun{
p <- 3
mu <- c(0.06, 0.08, 0.10)
set.seed(2); A <- matrix(rnorm(p*p), p); Sigma <- crossprod(A)/p
res <- f_mvskPortfolio(m1 = mu, M2 = Sigma, w0 = rep(1/p,p))
res$w
}

}
\seealso{
\code{\link{f_solvePortfolio}}, \code{\link{f_solveMVSKPortfolio}}
}
